// transform, cmdline "--fcp"

CASM fcp

// constant propergation

init initR

function af : -> Int
function bf : -> Int

rule initR =
{
	bf := BVadd_result(32, 1, 1)
	af := 2*3 + 5

    program(self) := undef
}
