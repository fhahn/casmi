// error annotation @4

rule main = {
  let x = peek( [ 1, "hallo" ] ) in skip
  //~^ type of 1 argument of `peek` is Tuple (Int,String,) but should be List(Unknown)

  program( self ) := undef
}
init main
