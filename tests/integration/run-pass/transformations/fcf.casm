// transform, cmdline "--fcf"

CASM fcf

// constant folding

init initR

function af : -> Int
function bf : -> Int

rule initR = 
{
	bf := BVadd_result(32, 1, 1)
	af := 2*3 + 5

    program(self) := undef
}
