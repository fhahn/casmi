// transform, cmdline "--cf --rle"

CASM RedundantLookupEliminationPass

init foo

function MEMORY : Int -> Int

rule foo = 
{
    let x = MEMORY(300) in
        MEMORY(10) := BVsb(32, 10, MEMORY( BVadd_result(32, BVadd_result(32, x, x), 3) ), 5)
}
